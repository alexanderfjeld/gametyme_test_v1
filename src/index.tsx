import React from "react";
import ReactDOM from "react-dom/client";
import FotballApp from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <FotballApp />
  </React.StrictMode>
);
